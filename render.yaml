services:
  - type: web
    name: qmessenger
    runtime: node
    plan: free
    region: fra
    buildCommand: npm ci
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
